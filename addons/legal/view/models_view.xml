<?xml version="1.0" encoding="utf-8"?>

<openerp>
    <data>

        
        <!-- FORM -->
        <record id="view_legal_models_form" model="ir.ui.view">
            <field name="name">legal.models.form</field>
            <field name="model">legal.models</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <field name="name"/>
                        <field name="description"/>
                        <field name="types"/>
                        <field name="attachment_ids"
                            widget="many2many_binary"
                            />
                    </group>
                </form>
            </field>
        </record>

        <!-- TREE -->
        <record id="view_legal_models_tree" model="ir.ui.view">
            <field name="name">legal.models.tree</field>
            <field name="model">legal.models</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <field name="description"/>
                </tree>
            </field>
        </record>

    </data>
</openerp>
<!-- vim:expandtab:smartindent:tabstop=4:softtabstop=4:shiftwidth=4
     -->
